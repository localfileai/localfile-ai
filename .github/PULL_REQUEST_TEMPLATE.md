<!-- 제목은 커밋 규칙과 동일하게: feat(rag): bge-m3로 임베딩 모델 교체
     squash merge 시 이 제목이 main의 커밋 메시지가 됩니다. -->

## 무엇을

<!-- 한두 줄. 무엇이 가능해졌는지 -->

Closes #

## 왜

<!-- 이 방법을 고른 이유. 다른 선택지를 검토했다면 왜 접었는지.
     diff에 없는 정보만 적으면 됩니다. -->

## 확인 방법

<!-- 리뷰어가 직접 돌려볼 수 있게. 명령어와 기대 결과 -->

```bash

```

## 체크리스트

- [ ] 로컬에서 검사를 통과했다 (`ruff check . && pytest -q` 또는 `npm run lint && npm run typecheck`)
- [ ] `main`을 rebase/merge해서 충돌을 해소했다
- [ ] 문서를 고쳐야 하는 변경이면 함께 고쳤다

## 다른 파트에 영향

- [ ] **없음** — 내 디렉터리 안에서 끝난다
- [ ] **있음** — 아래에 적는다

<!-- API 스키마, 응답 형식, IPC 채널, 환경 변수, 실행 명령이 바뀌었다면
     누가 무엇을 해야 하는지 적으세요. 안 적으면 다른 사람 브랜치가 조용히 깨집니다.

     예)
     - BE2: /api/search 응답에 `snippet` 필드 추가. 기존 필드는 그대로라 하위 호환됨
     - FE2: 미리보기 표에 신뢰도 컬럼을 넣으려면 이 PR 병합 후 재생성 필요 -->

## 스크린샷 / 로그

<!-- UI 변경이면 스크린샷, 성능·정확도 변경이면 수치.
     실험 결과라면 seed와 표본 수를 함께 적으세요. -->
