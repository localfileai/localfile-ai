# --- FastAPI 서버 ---
fastapi
uvicorn[standard]
pydantic>=2
eval_type_backport

# --- 문서 텍스트 추출 (BE2) ---
# PDF 첫 페이지
PyMuPDF
# HWP(구형 OLE 포맷). docx/pptx/hwpx는 표준 라이브러리 zipfile로 처리한다.
olefile

# --- AI / RAG (BE1) ---
# 벡터 저장·검색
chromadb
# Ollama HTTP 호출
requests

# --- 실험 스크립트 전용 ---
# 데이터셋 생성기가 PDF를 만들 때 사용
reportlab

# --- 테스트 (Ollama 없이 돈다) ---
pytest
# FastAPI TestClient가 사용
httpx
